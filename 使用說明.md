# ANIMA 雙平台入口 v0.2.2

本版提供 GPT-5.6 和 Sonnet 5 各自的系統提示，共用 v0.2.2 行為規格；角色與管線資料仍為 v0.2.0。兩者都只交付提示詞文字。尚未直接測試兩個目標模型，靜態檢查不是模型遵循率保證。

## 建議：專案用法

1. 在要使用的平台建立專案。
2. 專案指示貼入對應系統提示的全文，兩份擇一：GPT 用 `00_GPT_系統提示.md`；Claude 用 `00_Claude_系統提示.md`。
3. 專案檔案放入 `directing.md`、`characters.json`、`pipeline.json` 三份。
4. 在專案新對話直接輸入畫面需求，不必再上傳完整載入版。

若沿用舊專案，替換舊系統提示，不要保留另一份入口或舊完整載入版。本次必須替換 directing.md；characters.json 與 pipeline.json 未改，已放入者無須重複添加。

observations.md、驗收及維護資料平時不需載入。專案附件不代表模型每次都完整讀到；先用驗收案例確認，缺資料時應如實說明。

## 一般對話用法（與專案方式擇一）

GPT 上傳 `01_GPT_完整載入版.md`；Claude 上傳 `01_Claude_完整載入版.md`。告訴模型「請完整讀取附件，依此規格只編譯文字提示詞」，再開始給題目。兩份擇一，不混加分檔入口或舊版。新對話須重新提供完整載入版。

完整載入版已內嵌必要資料；若平台無法完整讀取，不能假裝已載入。它不需要和專案方式每回合一起使用。

## 兩版差異

| 項目 | GPT-5.6 | Sonnet 5 |
|---|---|---|
| 寫法 | 精簡任務與工具範圍 | 明確條件分支及適用範圍 |
| 主要針對問題 | 將畫面描述當成呼叫圖片工具的要求 | 字面理解時可能漏掉自主構圖或誤用格式限制 |
| 完成定義 | 提示詞文字完成即停止 | 每一案按分支完成文字即停止 |
| 共同邊界 | 只交付文字，允許必要唯讀查詢，禁止直接或間接出圖 | 相同 |
| 構圖、角色、尺度、prefix | 共用資料 | 相同 |

GPT 的圖片工具誤觸來自使用者本次回報，不代表所有 GPT 都有此缺陷。Claude 的分支式寫法是設計選擇，不表示它理解力較弱，也不保證不誤用工具。

設計依據：GPT-5.6 官方建議精簡重複指示、明定自主工作範圍及工具；Sonnet 5 官方指出其較字面遵循指令，適用範圍應明說。[GPT-5.6 指南](https://developers.openai.com/api/docs/guides/latest-model?model=gpt-5.6)、[Sonnet 5 指南](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-sonnet-5)。

## 驗證與維護

原42案加上8個本次修訂案例，共50案；兩平台均未實跑。新增案例要觀察工具活動，不能只看最後回覆是文字就判通過。舊模型案例說明中的「完整載入版」指此平台對應版本；測試時不要把預期答案提供給模型。

若部署端可移除圖片工具，移除可增加可執行的限制；提示詞不會改變工具權限。本包沒有修改平台設定或 ComfyUI 工作流。

入口來源為 `維護資料/入口共用規則.md` 加上 `GPT適配.md` 或 `Claude適配.md`，不要手改衍生的系統提示與完整載入版。改來源後執行：

```text
node 維護資料/重建與檢查.cjs
```

僅檢查是否過期則追加 `--check`。工具不連網、不呼叫模型。

入口版本是 v0.2.2，行為規格為 v0.2.2；角色與管線仍為 v0.2.0，刻意保留未改資料的版本。封存及 v020_歷史靜態結果.json 是歷史；新版結果見 GPT_靜態檢查結果.json、Claude_靜態檢查結果.json、平台入口檢查結果.json。模型結果仍為 NOT_RUN。

