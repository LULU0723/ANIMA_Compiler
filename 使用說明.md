# ANIMA 專案模式 v0.2.6

## 部署

1. 專案指示貼入對應的00_GPT_系統提示.md 或00_Claude_系統提示.md，兩份擇一。
2. 專案附件放 directing.md、characters.json、pipeline.json。
3. 從v0.2.5升級時替換 pipeline.json；00入口與 directing.md 只更新版本號，可一併替換；characters.json未改。
4. 把 ComfyUI 負向節點改為 pipeline.json → prompt_contract.negative.target（單行，不要換行）。v0.2.6 只在末尾多了 shiny skin。

01完整載入版自v0.2.3起停止維護，v0.2.2原檔已移入「維護資料/停用_v022完整載入版/」供歷史參考，不修改、不重建、不載入專案。它不代表現行規則；原完整載入版的資料差異與範例調整不再處理。

observations.md及維護、驗收資料不需常駐專案。characters.json沿用v0.2.3，保留tag-only策略；外觀補強仍由使用者提供。v0.2.6 負向末尾新增 shiny skin（使用者決定；單題實測見 observations.md SHINY-20260927。需要整片濕亮時，可在該張暫時從工作流負向移除）。v0.2.5起 prefix 改為官方建議形式，high contrast 移到角色／系列之後，負向補官方建議詞；工作流快照未重拍。

## 維護

00是衍生檔，修改維護資料/入口共用規則.md與對應平台適配來源後重建。其他正式檔各自直接維護。缺少入口來源時先提出建議並取得來源，不直接改00。不要載入整個歷史資料夾。

修改正式來源後，審核變更並更新維護資料/v026_source_manifest.json對應雜湊，再執行：

```text
node 維護資料/重建與檢查.cjs
node 維護資料/重建與檢查.cjs --check
```

重建只產生00入口及統一的驗收案例說明，不產生01。--check不改來源或衍生檔，但會寫入本次檢查報告。來源清單不由重建自動更新，避免把意外修改自動認可。observations.md也在清單內。

## 驗收

80案以驗收/模型測試案例.json為唯一來源；模型驗收案例.md由它產生，含所有案例及冒煙順序。冒煙清單涵蓋全部critical，需檢查工具活動，不能只看最後文字。模型紀錄維持NOT_RUN，靜態通過不等於模型遵規或成圖有效。

報告分passed、failed、skipped；來源路徑不存在或未設定時明確跳過，不算通過。封存檔可驗證，不代表外部原始目錄也已驗證。舊來源清單與v020歷史報告僅供追溯。

## 服裝搭配

不必知道服裝術語，也沒有必填選單。直接編譯仍是預設。

- 想先挑方向：「卡芙卡，成熟一點、黑絲襪，先給三套服裝方向。」→ 三套中文穿搭與效果，不附提示詞。
- 白話修改：「第二套，但太像上班穿的。」「保留上衣，其他重搭。」→ 只調整選定方案。
- 選定後：「好，給我提示詞。」→ 沿用該套與已確認修改，輸出一案。
- 懶得挑：「卡芙卡，黑色褲襪，服裝直接幫我搭好。」→ 自行決定並直接編譯。
- 只加一件：「原裝不變，只加外套。」→ 不重新搭整套。

服裝探索預設三套；/比較 仍是兩案完整提示詞，/討論 仍只有中文。/設計服裝 尚不是支援的指令，直接用上述自然語言即可。服裝提案不能用 /檢查 冒充提示詞驗收。新題不繼承探索模式。

本版先支援單人服裝規劃；原裝tag-only、人物外觀、內容限制、場景與道具邊界仍依原規則。三套提案有助選方向，不保證成圖美感；模型遵規與成圖效果仍待實測。
